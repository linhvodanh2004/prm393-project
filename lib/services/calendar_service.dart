// CalendarService now delegates entirely to RoomService (subcollection pattern).
// Canonical daily prices path: rooms/{roomId}/daily_prices/{yyyy-MM-dd}
export 'room_service.dart' show RoomService;
